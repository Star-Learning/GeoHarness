# 洪山区影像视觉巡检 · 发布包

推荐标题：**GeoHarness：一句话，让 GIS Agent 巡检洪山区**

本地视频：`hongshan-publish-20260905.mp4`（67 秒横屏发布版；MP4 不进入 Git）。

封面：[1920×1080 封面](hongshan-publish-20260905.cover.jpg)

## 可直接使用的简介

基于 DeepSeek Harness 做了一个 GIS Agent。这次输入一句话，看看它如何定位洪山区、叠加行政边界、
调用影像巡检工具，最终输出可控蒙版图层和中文报告。定位动画、处理中状态、图层与报告均来自真实运行。

本次经明确同意使用 2026-09-01 保存的真实地名与 OSM 边界缓存，重新请求 Esri 影像、重新计算结果；
并非重新在线查询边界。等待时间经过压缩。

这是 RGB 颜色与边缘启发式视觉初筛，不是训练过的遥感分割模型，也不是实测面积。模型文字仍需人工核查，
不能用报告中的方位描述或推测性解释替代地面验证。

标签：`#GeoHarness #GIS #DeepSeekHarness #AIAgent #卫星影像 #空间分析`

## 上传说明

- MP4 / H.264 High / 1920×1080 / 60 fps / AAC 双声道，可用于 B 站或小红书横屏视频。
- 配有步骤字幕、结果动态框和低音量原创合成配乐；无第三方音乐采样，没有配音。
- 60 fps 是导出帧率，源为目标 8 fps 的浏览器截图采样，并非原生 60 fps 录屏。
- 中途 140 张源帧为 2560×1440，其余 649 张为 1920×1080；统一输出 1080p，报告段增加同帧局部放大。
- 开始、关键阶段和收尾均使用同一个真实会话。截图接口超时造成的未采集等待段未被伪造补齐。
- 根据平台要求标注 AI 辅助内容；保留画面中的 Esri 和 OpenStreetMap 署名。未自动上传或发布。
- 地名缓存的后续分发/商业用途仍需核对 Esri 服务许可；OSM 边界遵循 ODbL，不能称为官方法定边界。

输入：

```text
全程中文巡检武汉市洪山区卫星影像，200字简报，附分类表与局限。
```

审计：[会话、来源、剪辑区间与视频校验](hongshan-publish-20260905.manifest.json)。
