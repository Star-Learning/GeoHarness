# Agent Video Prompt

新建一个空 Harness 会话，仅发送下面这条输入。数字结论必须由本轮 Tool Result 得出，不能把
Scenario 的 expected result 写进 Prompt。

```text
请从你当前真正可访问的官方 GIS 数据中，自主找到适合检查 Lower Manhattan 建筑的数据，并完成一次可审计的数据概览与质量检查。请报告数据发布方和快照日期、要素数量、坐标系、几何类型、空间范围、主要字段、缺失值和无效几何；再使用适合纽约的米制投影计算建筑占地面积，并汇总面积与建筑高度的最小值、中位数、平均值和最大值。把原始建筑和必要的派生图层显示在地图上，最后用简洁中文说明这份数据是否适合继续做米制空间分析以及判断依据。所有数字、图层和结论都必须来自本次真实工具调用，不要套用预设示例。
```

录制重点：数据发现、建筑图层载入、质量检查、面积计算、地图适配范围、最终统计与判断。
